"use client";
import { useLocation } from 'react-router-dom';

export default function EditImage() {
    const location  = useLocation();
    return (
        <div>
            <h1>Edit Image</h1>
        </div>
    )
}